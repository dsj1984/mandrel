---
description: >-
  Redirect stub — the decompose half of Epic planning now lives in the
  author + persist steps of `helpers/plan-epic.md`. Kept one release so
  pre-existing links still resolve; scheduled for deletion (Epic #4474 PR7).
---

# Epic plan — decompose phase (redirect stub)

> **Superseded (Epic #4474).** The 12-phase pipeline this file pointed into
> was collapsed to three steps. Ticket authoring (`tickets.json`) and the
> critic passes are the **author** step, and the validator / DAG /
> file-assumption gates + story creation are the **persist** step, of
> [`helpers/plan-epic.md`](plan-epic.md). Read that file — do not resurrect
> an inline copy here. This stub is removed in the next release.
