{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/dsj1984/mandrel/blob/main/.agents/schemas/epic-spec.schema.json",
  "version": "2.0.0",
  "title": "Epic Spec (declarative structural SSOT)",
  "description": "Canonical structural specification for an Epic and its children (Features, Stories, Tasks), consumed by the epic-spec-reconciler (Epic #1182). Lives on disk as .agents/epics/<epic-id>.yaml. The reconciler diffs this spec against live GitHub state and applies structural mutations (Create/Update/Close/Relink) behind a dry-run default. Slugs are stable, GH-incarnation-independent identifiers used for intra-spec dependency edges (dependsOn) and the spec ↔ GH-issue mapping persisted in the sibling .agents/epics/<epic-id>.state.json. This schema describes the structural shape only; agent-execution state (agent::* labels) is intentionally not modelled — that surface belongs to the wave-runner, not the reconciler.",
  "type": "object",
  "additionalProperties": false,
  "required": ["epic", "features"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Relative or absolute reference to this schema file. Allowed at the top level so YAML editors with $schema-aware autocomplete (e.g. the Red Hat YAML extension) can resolve the schema from the spec file itself."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Optional schema-shape identifier (Epic #3078). Identification-only — readers MUST NOT branch behavior on this value. Bumped to 2.0.0 when Story-level inline acceptance[]/verify[] arrays were introduced and tasks[] became optional under the 3-tier collapse. A spec without `version` is interpreted as the legacy 4-tier shape; the schema accepts both because the change is purely additive."
    },
    "epic": {
      "$ref": "#/$defs/epic",
      "description": "The Epic this spec describes. Carries the GH issue number, human title, and structural labels (type::epic plus any project-specific structural tags). Execution-state labels (agent::*) MUST NOT appear here."
    },
    "features": {
      "type": "array",
      "description": "Ordered list of Features under this Epic. Order is preserved for human readability but is not load-bearing — wave ordering is driven by stories[].wave + dependsOn, not by feature position.",
      "items": { "$ref": "#/$defs/feature" }
    },
    "gates": {
      "$ref": "#/$defs/gates",
      "description": "Optional references to baseline + gate-config slugs resolved by the baseline subsystem. Carried in the spec so structural changes can be reviewed alongside the quality-gate config that will run against them."
    }
  },
  "$defs": {
    "slug": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Stable kebab-case identifier used for intra-spec dependency edges (dependsOn) and the spec ↔ GH-issue mapping. Slugs must be unique within their scope (features within an epic, stories within a feature, tasks within a story). Slugs are not GH issue numbers — they survive Epic re-incarnations and ticket renumbering. Pattern: lowercase alphanumeric + hyphens, must start with [a-z0-9]."
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "description": "A GitHub label name. Structural labels only (e.g. type::epic, type::feature, type::story, type::task, persona::*, area::*). Execution-state labels in the AGENT_LABELS allow-list (agent::*) MUST NOT appear in the spec — they are owned by the wave-runner and the reconciler refuses to write them."
    },
    "labels": {
      "type": "array",
      "description": "Structural labels attached to this entity. The reconciler enforces an allow-list (STRUCTURAL_LABELS) and rejects any plan that attempts to update AGENT_LABELS via the structural path.",
      "items": { "$ref": "#/$defs/label" },
      "uniqueItems": true
    },
    "epic": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "title"],
      "description": "Top-level Epic descriptor. Unlike Features/Stories/Tasks (which are slug-keyed and may not exist in GH yet), the Epic always has a real GH issue number — the spec lives at .agents/epics/<id>.yaml and that <id> is the same number used here.",
      "properties": {
        "id": {
          "type": "integer",
          "minimum": 1,
          "description": "GitHub issue number for the Epic. Authoritative — the spec file's filename derives from this value (.agents/epics/<id>.yaml) and the sibling state file (<id>.state.json) is keyed against it."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable Epic title. Reconciler updates the GH issue title to match this value on every apply (subject to mayUpdate gating)."
        },
        "body": {
          "type": "string",
          "description": "Optional Epic body. When present, the reconciler updates the GH issue body to match. When omitted, the GH issue body is left untouched (allowing operator-curated bodies to coexist with structural reconciliation). Multi-line markdown is fully supported."
        },
        "labels": {
          "$ref": "#/$defs/labels",
          "description": "Structural labels attached to the Epic (e.g. type::epic plus any project-specific structural tags). agent::* labels are forbidden — they are owned by the wave-runner."
        }
      }
    },
    "feature": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "title", "stories"],
      "description": "A Feature groups Stories under a common product/architectural theme. Features themselves are GitHub issues (type::feature) when materialised by the reconciler — they carry no execution state, only structure.",
      "properties": {
        "slug": {
          "$ref": "#/$defs/slug",
          "description": "Stable kebab-case identifier for this Feature. Must be unique within the parent Epic's features[] array."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable Feature title."
        },
        "body": {
          "type": "string",
          "description": "Optional Feature body. Same semantics as epic.body — written by the reconciler when present, left untouched when omitted."
        },
        "labels": {
          "$ref": "#/$defs/labels",
          "description": "Structural labels attached to this Feature (e.g. type::feature plus any project-specific structural tags)."
        },
        "stories": {
          "type": "array",
          "description": "Ordered list of Stories under this Feature. Order is for readability; execution order is driven by stories[].wave + dependsOn.",
          "items": { "$ref": "#/$defs/story" }
        }
      }
    },
    "story": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "title", "wave"],
      "description": "A Story is the unit of agent execution — exactly one Story branch (story-<id>) and exactly one Story sub-agent per Story per wave. Stories declare their wave assignment and inter-Story dependencies via slug references; the wave-runner consumes these to schedule the parallel fan-out. Stories may carry either child tasks[] (legacy 4-tier decomposition) and/or inline acceptance[] + verify[] arrays (3-tier collapse, Epic #3078). Both shapes validate; tasks[] is kept optional so 3-tier Stories that decompose acceptance/verify directly are accepted, and 4-tier Stories with tasks[] continue to validate with no regression.",
      "properties": {
        "slug": {
          "$ref": "#/$defs/slug",
          "description": "Stable kebab-case identifier for this Story. Must be unique within the parent Feature's stories[] array, and is the value referenced by sibling Stories' dependsOn arrays."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable Story title. Becomes the GH issue title."
        },
        "body": {
          "type": "string",
          "description": "Optional Story body. Written by the reconciler when present; left untouched when omitted. Used to carry Story-level acceptance criteria / context that the decomposer authored."
        },
        "wave": {
          "type": "integer",
          "minimum": 0,
          "description": "Zero-indexed wave number. The wave-runner groups Stories with the same wave value into a single parallel batch and waits for every Story in wave N to close before dispatching wave N+1. Wave 0 typically contains Stories with no inbound dependsOn edges."
        },
        "dependsOn": {
          "type": "array",
          "description": "Slugs of Stories (within this Epic) that must close before this Story may dispatch. Slugs reference stories[].slug values — not GH issue numbers — so the spec survives GH re-incarnations. The reconciler resolves slugs to issue numbers via the sibling state file when writing GH-side `blocked by` references.",
          "items": {
            "$ref": "#/$defs/slug",
            "description": "Slug of a sibling Story that must close before this Story may dispatch."
          },
          "uniqueItems": true
        },
        "labels": {
          "$ref": "#/$defs/labels",
          "description": "Structural labels attached to this Story (e.g. type::story, persona::*). agent::* labels are forbidden — they are owned by the wave-runner at dispatch time."
        },
        "acceptance": {
          "type": "array",
          "description": "Optional inline acceptance criteria for this Story (Epic #3078 — 3-tier collapse). Each entry is a single user-visible acceptance statement that the Story-level agent must satisfy. Coexists with tasks[]; when both are present, tasks[] carries the implementation decomposition and acceptance[] carries the Story-level outcome contract. When only acceptance[] + verify[] are present (no tasks[]), the Story sub-agent treats them as the Goal/Changes/Acceptance/Verify surface that previously lived on a child Task body.",
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "A single acceptance criterion expressed as a user-visible outcome (e.g. 'A Story object containing inline acceptance[] validates against the schema')."
          }
        },
        "verify": {
          "type": "array",
          "description": "Optional inline verification commands for this Story (Epic #3078 — 3-tier collapse). Each entry is a shell-runnable command (typically a contract test invocation) that proves the corresponding acceptance[] entries hold. Coexists with tasks[]; semantics mirror the existing Task-body `## Verify` section.",
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "A single verification command (e.g. 'node --test tests/scripts/epic-spec-schema.test.js')."
          }
        },
        "tasks": {
          "type": "array",
          "description": "Ordered list of Tasks under this Story (legacy 4-tier decomposition). The Story sub-agent walks tasks[] in order during /story-deliver; each Task produces exactly one commit on the Story branch via task-commit.js. Optional as of Epic #3078: Stories that use inline acceptance[]/verify[] may omit tasks[] entirely.",
          "items": { "$ref": "#/$defs/task" }
        }
      }
    },
    "task": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "title"],
      "description": "A Task is the smallest unit of agent work — exactly one commit on the parent Story's branch. Tasks have no wave/dependsOn surface: ordering within a Story is purely positional (the order they appear in stories[].tasks), and cross-Task dependencies are expressed implicitly via that ordering. Cross-Story or cross-Wave dependencies belong on the parent Story's dependsOn, not on the Task.",
      "properties": {
        "slug": {
          "$ref": "#/$defs/slug",
          "description": "Stable kebab-case identifier for this Task. Must be unique within the parent Story's tasks[] array."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable Task title. Becomes the GH issue title and the conventional-commit subject prefix."
        },
        "body": {
          "type": "string",
          "description": "Optional Task body. Written by the reconciler when present; left untouched when omitted. Typically carries the Task's Goal/Changes/Acceptance/Verify sections from the decomposer."
        },
        "labels": {
          "$ref": "#/$defs/labels",
          "description": "Structural labels attached to this Task (e.g. type::task, persona::*). agent::* labels are forbidden — they are owned by the wave-runner."
        }
      }
    },
    "gates": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional references to baseline + gate-config slugs. Carried in the spec so structural changes can be reviewed alongside the quality-gate config that will run against them. Resolved to baseline-service inputs by the existing baseline subsystem; opaque to the reconciler itself.",
      "properties": {
        "baseline": {
          "type": "string",
          "minLength": 1,
          "description": "Slug naming the baseline ratchet snapshot this Epic targets (e.g. ratchet-baseline-2026-05-12). The baseline subsystem resolves this to concrete lint/mi/crap baseline files at close-validation time."
        },
        "config": {
          "type": "string",
          "minLength": 1,
          "description": "Slug naming the gate-config version this Epic uses (e.g. gate-config-v6). Allows different Epics to opt into different gate configurations during long-lived migrations."
        }
      }
    }
  }
}
