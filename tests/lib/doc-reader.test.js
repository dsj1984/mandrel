// This file intentionally has no tests.
//
// The dead-code exports it previously covered were removed as leftovers of
// Epic #4429 Story #4433's digest-first cutover (Story #4455, refs #4444).
// The remaining live export from `doc-reader.js` (`readDocFiles`) is
// exercised indirectly through `ensureDocsDigest` coverage in
// `tests/plan-docs-digest.test.js`.
